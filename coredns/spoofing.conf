# =============================================================================
# Rogue DNS Spoofing Configuration
# =============================================================================
# Format: <domain>    <spoofed_ip>
# One domain per line. Lines starting with # are comments.
# After editing, run:  sudo bash /etc/coredns/setup_coredns.sh
# =============================================================================

google.com          203.0.113.1
bancosantander.es   203.0.113.2
paypal.com          203.0.113.3
microsoft.com       203.0.113.4
amazon.com          203.0.113.5

# Add more domains below:
# mybank.com        203.0.113.10
